# Use the official Nginx image
FROM nginx:latest

# Copy a default Nginx index file or a custom one
COPY ./index.html /usr/share/nginx/html/index.html

# Expose port 80 (inside the container)
EXPOSE 80
